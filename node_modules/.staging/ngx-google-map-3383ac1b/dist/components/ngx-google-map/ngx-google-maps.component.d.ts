import { OnInit, EventEmitter } from '@angular/core';
export declare class NgxGoogleMapComponent implements OnInit {
    mapType: string;
    multiplePlaces: boolean;
    mapClick: EventEmitter<any>;
    markerClick: EventEmitter<any>;
    locationSelected: EventEmitter<any>;
    private mapElement;
    private searchInput;
    private doneButton;
    private mapOptions;
    private mapInstance;
    private mapSearchBox;
    private markers;
    private infoWindow;
    private selectedLocations;
    constructor();
    ngOnInit(): void;
    private initMap();
    private initMapEvents();
    private addDoneButton();
    private placeMarkerAndPanTo(latLang);
    private addDragToMarker(marker);
    private getMarketLocation(marker);
    private addClickToMarker(marker);
    private addSearchBox();
    private getFromattedAddress(geocoder, latlang, infowindow);
    private addSearchEvent();
    private clearMarkers();
    private searchPlace();
}
